#calls all of the INSERT fiels through one file

import zip as z1



def calling_all():
    #adding zipcodes
    #the below would probably never be called until one is calling it for the first time
    zip.main()

    #adding users
